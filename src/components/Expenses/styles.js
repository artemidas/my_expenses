import { css } from "@emotion/core";

const list = css`
  color: red;
`;

const form = css`
  display: flex;
`;

const input = css`
  display: flex;
  flex-direction: column;
`;

export default { form, input, list };
